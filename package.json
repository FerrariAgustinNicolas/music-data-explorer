{
  "name": "music-data-explorer",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace server && npm run build --workspace client",
    "lint": "npm run lint --workspace server && npm run lint --workspace client",
    "test:api": "npm run test --workspace server",
    "test:ui": "npm run test --workspace client",
    "test:e2e": "playwright test",
    "test": "npm run test:api && npm run test:ui && npm run test:e2e"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.1",
    "concurrently": "^8.2.2"
  }
}
